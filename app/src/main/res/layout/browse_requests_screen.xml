<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/browse_results">

            <Button
                android:layout_width="80dp"
                android:layout_height="30dp"
                android:id="@+id/home_button"
                android:layout_alignTop="@id/search_by_bar"
                android:layout_alignBottom="@id/search_by_bar"
                android:background="@drawable/home"
                android:onClick="goHome"/>

            <Button
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_centerHorizontal="true"
                android:layout_toRightOf="@id/home_button"
                android:id="@+id/search_by_bar"
                android:text="Search By..."
                android:textSize="30sp"
                android:background="@color/green"
                android:padding="10dp"
                android:layout_marginTop="20dp"
                android:layout_marginBottom="20dp"
                android:layout_marginRight="20dp"
                android:onClick="searchBy"
                />


        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/browse_results2"
            android:layout_below="@id/search_by_bar"
            android:orientation="vertical"
            >

        </LinearLayout>

    </RelativeLayout>

</ScrollView>